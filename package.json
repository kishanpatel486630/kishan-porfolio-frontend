{
  "name": "new-update-portfolio",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "setup": "npm install --prefix backend && npm install --prefix client && npm install",
    "dev": "concurrently -n backend,client -c cyan,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix backend",
    "dev:client": "npm run dev --prefix client"
  },
  "dependencies": {
    "nodemailer": "^6.10.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
